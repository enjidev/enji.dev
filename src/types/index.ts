export type TLastUpdate = {
  commiter: string;
  message: string;
  avatar: string;
  date: string;
  url: string;
};

export type TTableOfContentsItem = {
  title: string;
  depth: number;
  slug: string;
};
